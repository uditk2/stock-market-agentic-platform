# W14 Pipeline Storage Tasks (Iteration 2)

1. Confirm target workflow identity (`Build Installers`) and failing run IDs.
2. Create issue draft + GitHub issue for retention policy change.
3. Enumerate and delete stale workflow runs older than latest 3.
4. Add daily scheduled cleanup workflow with `workflow_dispatch` fallback.
5. Validate with local YAML check and GitHub manual dispatch.
6. Update project context artifacts and completion message.
